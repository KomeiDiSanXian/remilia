coverage:
  range: 60..80
  round: down
  precision: 2
  status:
    project:
      default:
        target: auto
        threshold: 3%
    patch:
      default:
        target: auto
        threshold: 10%

comment:
  layout: header, diff, flags, files, footer
  behavior: default
  require_changes: true
