bot:
  qq:
    app_id: 123456
    bot_id: 654321
    token: "YOUR_TOKEN"
    secret: "YOUR_SECRET"

server:
  host: "0.0.0.0"
  port: 8080

log:
  level: "info"    # 修改这个值测试热更新: debug, info, warn, error
  format: "text"

middleware:
  backpressure:
    limit: 100       # 修改这个值测试热更新

middleware:
  logging: true    # 修改这个值测试热更新
  recover: true
